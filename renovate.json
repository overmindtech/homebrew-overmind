{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>overmindtech/renovate-config"
  ],
  "prBodyNotes": "Once all tests pass, add a label with `pr-pull`. This will trigger a workflow that pulls the compiled bottles into the current release and closes the PR. **Do not merge PRs directly.**",
  "automerge": false,
  "packageRules": [
    {
      "matchPackageNames": ["overmindtech/cli"],
      "automerge": false
    }
  ]
}
